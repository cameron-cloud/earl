# Speed up dev builds
[profile.dev]
opt-level = 0          # No optimization for app code (fast compile)
incremental = true

[profile.dev.package."*"]
opt-level = 1          # Optimize dependencies (they rarely change)

# Smaller, faster release builds
[profile.release]
lto = "thin"
strip = true
opt-level = 2
